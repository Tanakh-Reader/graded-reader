// prettier.config.js
module.exports = {
	plugins: ["prettier-plugin-tailwindcss"],
	printWidth: 80, // Sets the maximum line length
	useTabs: true, // If true, it uses tabs; otherwise, it uses spaces
	tabWidth: 2, // Sets the number of spaces per indentation-level
};
